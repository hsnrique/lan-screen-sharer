[package]
name = "screen-sender"
version = "1.0.0"
edition = "2021"

[dependencies]
scrap = "0.5"
image = { version = "0.25", default-features = false, features = ["jpeg"] }
local-ip-address = "0.6"
ctrlc = "3.4"

[profile.release]
opt-level = 3
lto = true
