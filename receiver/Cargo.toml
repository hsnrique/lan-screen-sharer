[package]
name = "screen-receiver"
version = "1.0.0"
edition = "2021"

[dependencies]
image = { version = "0.25", default-features = false, features = ["jpeg"] }
minifb = "0.27"

[profile.release]
opt-level = 3
lto = true
